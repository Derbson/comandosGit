
git init --bare   >    criar um repositório local

git add	    >    guarda arquivo numa area de espera

git commit -m "titulo da commit"  >  adicinar arquivo ao repositório local

git status  >	status do arquivo

git remote add origin https://github.com/

CONFIGURAÇÕES INICIAIS:

git config --list

git config --global user.email "email_adress@server.com"

git config --global user.name	"nome de usuário do github"


